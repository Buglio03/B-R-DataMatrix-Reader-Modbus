# B-R-DataMatrix-Reader-Modbus
This project contains a Structured Text implementation designed for use with B&amp;R Automation Studio.   It allows a PLC to interface with a Cognex DataMan camera via Modbus TCP/IP, trigger a read request, and decode the resulting DataMatrix string into readable ASCII format.
